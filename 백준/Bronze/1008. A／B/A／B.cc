#include <bits/stdc++.h>
using namespace std;
#define ll long long

double a,b;

int main() {
    ios::sync_with_stdio(0), cin.tie(0);

    cin>>a>>b;
    cout.precision(10);
    cout<<a/b;
    return 0;
}