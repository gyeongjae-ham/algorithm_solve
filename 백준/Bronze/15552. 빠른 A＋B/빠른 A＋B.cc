#include <bits/stdc++.h>
using namespace std;
int T,a,b,i;

int main(){
    cin.tie(NULL); 
    ios_base::sync_with_stdio(false);
    cin>>T;
    for(i=0; i<T; i++){
        cin>>a>>b;
        cout<<a+b<<"\n";
    }
    return 0;
}