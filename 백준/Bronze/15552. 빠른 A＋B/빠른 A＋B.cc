#include <bits/stdc++.h>

#define fi cin.tie(0)->sync_with_stdio(0)
#define ll long long

using namespace std;

int main() {
  fi;
    
  int n, a, b;
  cin>>n;
  for(int i=0;i<n;i++) {
    cin>>a>>b;
    cout<<a+b<<"\n";
  } 
  
  return 0;
}