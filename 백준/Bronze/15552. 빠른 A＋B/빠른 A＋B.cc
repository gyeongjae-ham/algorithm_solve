#include <bits/stdc++.h>
using namespace std;
#define X first
#define Y second

int t;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin>>t;
    for(int i=0;i<t;i++){
        int a,b;
        cin>>a>>b;
        cout<<a+b<<"\n";
    }
    
    return 0;
}