#include <bits/stdc++.h>

using namespace std;

int a,b,c;
string f,f2;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin>>a>>f>>b>>f2>>c;
    
    if(a+b==c) {
        cout<<"YES"<<"\n";
        return 0;
    }
    cout<<"NO"<<"\n";
    
    
    return 0;
}