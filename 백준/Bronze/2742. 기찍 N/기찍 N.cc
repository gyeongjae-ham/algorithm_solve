#include <bits/stdc++.h>
using namespace std;
int N,i;

int main(){
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    cin>>N;
    for(i=N; i>=1; i--){
        cout<<i<<"\n";
    }
    return 0;
}