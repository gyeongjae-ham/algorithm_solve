#include <iostream>
#include <string>

using namespace std;

int al[26];
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    
    string s;
    cin>>s;
    
    cout << s.size();
    
    return 0;
}