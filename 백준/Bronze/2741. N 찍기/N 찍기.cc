#include <bits/stdc++.h>
using namespace std;
int N,i;

int main(){
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    cin>>N;
    for(i=1; i<=N; i++){
        cout<<i<<"\n";
    }
    return 0;
}