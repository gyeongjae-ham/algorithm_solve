#include <bits/stdc++.h>
using namespace std;
int cnt=0;
string s;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    while(cin>>s) {
        cnt++;
    }
    cout<<cnt;
    return 0;
}