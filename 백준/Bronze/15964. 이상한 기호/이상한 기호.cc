#include <bits/stdc++.h>

using namespace std;

long long a,b;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    cin>>a>>b;
    
    cout<<(a+b)*(a-b)<<"\n";
    
    return 0;
}